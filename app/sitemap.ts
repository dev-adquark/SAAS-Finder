import type {MetadataRoute} from "next"; import {products,categories} from "@/lib/data"; export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000"; return [{url:base},{url:base+"/products"},{url:base+"/categories"},{url:base+"/methodology"},{url:base+"/disclosure"},...products.map(p=>({url:base+"/products/"+p.slug})),...categories.map(c=>({url:base+"/categories/"+c.toLowerCase().replace(/[^a-z0-9]+/g,"-")})),...products.filter(p=>p.alternatives.length).map(p=>({url:base+"/alternatives/"+p.slug}))]}