import ProductCard from "@/components/product-card"; import {products} from "@/lib/data"; export const metadata={title:"SaaS Products"}; export default function Products(){return <main className="container py-14"><p className="accent">PRODUCT CATALOG</p><h1 className="text-4xl font-bold mt-2">SaaS products</h1><p className="muted mt-3">Browse structured editorial profiles. Live pricing is linked to vendors until independently verified.</p><div className="grid md:grid-cols-3 gap-5 mt-8">{products.map(p=><ProductCard key={p.slug} product={p}/>)}</div></main>}