import {getProducts} from "@/lib/catalog";import {SearchProducts} from "@/components/search-products";export const metadata={title:"SaaS products"};export default async function Products(){const products=await getProducts();return <section className="section"><div className="container"><span className="eyebrow">Product index</span><h1>Browse SaaS products</h1><p className="section-intro">Search by product, category or use case.</p><SearchProducts products={products}/></div></section>}