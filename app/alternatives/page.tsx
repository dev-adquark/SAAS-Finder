import Link from "next/link";import {products} from "@/lib/data";export const metadata={title:"SaaS alternatives"};export default function Alternatives(){return <section className="section"><div className="container"><span className="eyebrow">Alternatives engine</span><h1>Alternatives to popular SaaS tools</h1><p className="section-intro">Curated alternatives pages organized by product and category.</p><div className="grid">{products.map(p=><Link className="card" key={p.slug} href={"/alternatives/"+p.slug}><span className="tag">{p.category}</span><h2>Alternatives to {p.name}</h2><p className="muted">{p.alternatives.length} curated alternative{p.alternatives.length===1?"":"s"}.</p></Link>)}</div></div></section>}