import { NextResponse } from "next/server";
import { db } from "@/lib/db";
import { requireAdmin } from "@/lib/admin-auth";

export async function GET(req: Request,{params}:{params:Promise<{id:string}>}){if(!requireAdmin(req))return NextResponse.json({error:"Unauthorized"},{status:401});const {id}=await params;return NextResponse.json(await db.faq.findMany({where:{productId:id},orderBy:{sortOrder:"asc"}}))}
export async function POST(req: Request,{params}:{params:Promise<{id:string}>}){if(!requireAdmin(req))return NextResponse.json({error:"Unauthorized"},{status:401});const {id}=await params;try{const b=await req.json();if(typeof b.question!=="string"||!b.question.trim()||typeof b.answer!=="string"||!b.answer.trim())return NextResponse.json({error:"question and answer are required"}, {status:400});if(b.sortOrder!==undefined&&(!Number.isInteger(b.sortOrder)||b.sortOrder<0))return NextResponse.json({error:"sortOrder must be a non-negative integer"},{status:400});return NextResponse.json(await db.faq.create({data:{productId:id,question:b.question.trim(),answer:b.answer.trim(),sortOrder:b.sortOrder??0}}),{status:201})}catch{return NextResponse.json({error:"Invalid FAQ payload or product is invalid"},{status:400})}}
