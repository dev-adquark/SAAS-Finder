import { NextResponse } from "next/server";
import { db } from "@/lib/db";
import { requireAdmin } from "@/lib/admin-auth";

export async function GET(req: Request,{params}:{params:Promise<{id:string}>}){if(!requireAdmin(req))return NextResponse.json({error:"Unauthorized"},{status:401});const {id}=await params;return NextResponse.json(await db.alternative.findMany({where:{productId:id},include:{alternative:true},orderBy:{sortOrder:"asc"}}))}
export async function POST(req: Request,{params}:{params:Promise<{id:string}>}){if(!requireAdmin(req))return NextResponse.json({error:"Unauthorized"},{status:401});const {id}=await params;try{const b=await req.json();if(typeof b.alternativeId!=="string"||!b.alternativeId.trim())return NextResponse.json({error:"alternativeId is required"},{status:400});if(b.alternativeId===id)return NextResponse.json({error:"A product cannot be its own alternative"},{status:400});if(b.sortOrder!==undefined&&(!Number.isInteger(b.sortOrder)||b.sortOrder<0))return NextResponse.json({error:"sortOrder must be a non-negative integer"},{status:400});const target=await db.product.findUnique({where:{id:b.alternativeId},select:{id:true}});if(!target)return NextResponse.json({error:"alternative product not found"},{status:404});return NextResponse.json(await db.alternative.create({data:{productId:id,alternativeId:b.alternativeId,sortOrder:b.sortOrder??0}}),{status:201})}catch{return NextResponse.json({error:"Alternative already exists or product is invalid"},{status:409})}}
