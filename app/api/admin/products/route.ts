import {NextResponse} from "next/server";import {db} from "@/lib/db";
function authorized(req:Request){const expected=process.env.ADMIN_API_KEY;return Boolean(expected&&req.headers.get("authorization")==="Bearer "+expected)}
export async function GET(req:Request){if(!authorized(req))return NextResponse.json({error:"Unauthorized"},{status:401});const products=await db.product.findMany({include:{category:true}});return NextResponse.json(products)}
export async function POST(req:Request){if(!authorized(req))return NextResponse.json({error:"Unauthorized"},{status:401});const body=await req.json();if(!body.slug||!body.name||!body.category||!body.officialUrl)return NextResponse.json({error:"slug, name, category and officialUrl are required"},{status:400});const category=await db.category.upsert({where:{slug:String(body.category).toLowerCase().replace(/[^a-z0-9]+/g,"-")},update:{name:body.category},create:{name:body.category,slug:String(body.category).toLowerCase().replace(/[^a-z0-9]+/g,"-")}});const product=await db.product.create({data:{slug:body.slug,name:body.name,categoryId:category.id,tagline:body.tagline??"",description:body.description??"",subcategory:body.subcategory??null,rating:body.rating??null,officialUrl:body.officialUrl,pricingUrl:body.pricingUrl??null,bestFor:body.bestFor??[],features:body.features??[],pros:body.pros??[],cons:body.cons??[],status:body.status??"DRAFT"}});return NextResponse.json(product,{status:201})}