import type { Metadata } from "next";import Link from "next/link";import {getCategories,getProducts} from "@/lib/catalog";import {ProductCard} from "@/components/product-card";import {absolute} from "@/lib/site";
export const metadata:Metadata={title:"SaaS reviews, alternatives and comparisons",description:"Structured SaaS reviews, pricing snapshots, alternatives and side-by-side comparisons for software buyers.",alternates:{canonical:absolute("/")},openGraph:{title:"SaaS reviews, alternatives and comparisons",description:"Structured SaaS reviews, pricing snapshots, alternatives and side-by-side comparisons for software buyers.",url:absolute("/"),type:"website"},twitter:{card:"summary_large_image",title:"SaaS reviews, alternatives and comparisons",description:"Structured SaaS research for software buyers."}};
export default async function Home(){const[products,categories]=await Promise.all([getProducts(),getCategories()]);const jsonLd={"@context":"https://schema.org","@type":"WebSite",name:"SaaSFinder",url:absolute("/"),description:"Structured SaaS reviews, alternatives and comparisons."};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><section className="hero"><div className="container"><span className="eyebrow">Independent SaaS research</span><h1>Compare software without digging through ten vendor tabs.</h1><p>Structured reviews, pricing snapshots, pros and cons, use cases, alternatives and side-by-side comparisons. Commercial links are kept separate from editorial methodology.</p><div className="actions"><Link className="btn primary" href="/products">Browse all products →</Link><Link className="btn secondary" href="/comparisons">See comparisons</Link></div><div className="kpi"><div className="card"><strong>{products.length}</strong><span className="muted">published products</span></div><div className="card"><strong>{categories.length}</strong><span className="muted">categories</span></div><div className="card"><strong>Fresh</strong><span className="muted">dated snapshots</span></div><div className="card"><strong>Clear</strong><span className="muted">commercial labels</span></div></div></div></section><section className="section"><div className="container"><h2>Featured software</h2><p className="section-intro">Start with a category or open a full review.</p><div className="grid">{products.map(p=><ProductCard key={p.slug} product={p}/>)}</div></div></section><section className="section"><div className="container"><h2>Browse categories</h2><div className="grid">{categories.map(c=><Link className="card" key={c} href={"/categories/"+encodeURIComponent(c.toLowerCase())}><span className="tag">Category</span><h2>{c}</h2><p className="muted">Reviews, alternatives and comparisons for {c.toLowerCase()}.</p></Link>)}</div></div></section></>}