import {NextResponse} from "next/server";import {getProduct} from "@/lib/data";import {absolute} from "@/lib/site";export async function GET(req:Request,{params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=getProduct(slug);if(!p)return NextResponse.redirect(absolute("/products"));const url=p.affiliateAvailable&&p.affiliateUrl?p.affiliateUrl:p.officialUrl;const out=new URL(url);out.searchParams.set("utm_source","saasfinder");out.searchParams.set("utm_medium","affiliate");out.searchParams.set("utm_campaign","product_review");return NextResponse.redirect(out.toString(),{status:302})}