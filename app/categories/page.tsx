import Link from "next/link"; import {categories,products} from "@/lib/data"; export const metadata={title:"SaaS Categories"}; export default function Categories(){return <main className="container py-14"><h1 className="text-4xl font-bold">SaaS categories</h1><div className="grid md:grid-cols-3 gap-5 mt-8">{categories.map(c=>{const slug=c.toLowerCase().replace(/[^a-z0-9]+/g,"-"); return <Link className="panel p-6" key={c} href={"/categories/"+slug}><h2 className="text-xl font-semibold">{c}</h2><p className="muted mt-2">{products.filter(p=>p.category===c).length} products</p></Link>})}</div></main>}