import Link from "next/link";import {getCategories,getCategoryProducts} from "@/lib/catalog";export const metadata={title:"SaaS categories"};export default async function Categories(){const categories=await getCategories();return <section className="section"><div className="container"><span className="eyebrow">Category hubs</span><h1>Browse software by category</h1><div className="grid">{await Promise.all(categories.map(async c=><Link className="card" key={c} href={"/categories/"+encodeURIComponent(c.toLowerCase())}><span className="tag">{(await getCategoryProducts(c)).length} products</span><h2>{c}</h2><p className="muted">Reviews, alternatives and comparisons for {c.toLowerCase()}.</p></Link>))}</div></div></section>}