import type { Metadata } from "next";import Link from "next/link";import {getCategories,getCategoryProducts} from "@/lib/catalog";import {absolute} from "@/lib/site";export const metadata:Metadata={title:"SaaS categories",description:"Browse SaaS software by category with reviews, alternatives and comparisons.",alternates:{canonical:absolute("/categories")},openGraph:{title:"SaaS categories",description:"Browse SaaS software by category with reviews, alternatives and comparisons.",url:absolute("/categories")}};export default async function Categories(){const categories=await getCategories();const jsonLd={"@context":"https://schema.org","@type":"CollectionPage",name:"SaaS categories",url:absolute("/categories")};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><section className="section"><div className="container"><span className="eyebrow">Category hubs</span><h1>Browse software by category</h1><div className="grid">{await Promise.all(categories.map(async c=><Link className="card" key={c} href={"/categories/"+encodeURIComponent(c.toLowerCase())}><span className="tag">{(await getCategoryProducts(c)).length} products</span><h2>{c}</h2><p className="muted">Reviews, alternatives and comparisons for {c.toLowerCase()}.</p></Link>))}</div></div></section></>}