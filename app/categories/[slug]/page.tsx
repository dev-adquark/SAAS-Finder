import {notFound} from "next/navigation"; import {categories,getCategoryProducts} from "@/lib/data"; import ProductCard from "@/components/product-card"; export function generateStaticParams(){return categories.map(c=>({slug:c.toLowerCase().replace(/[^a-z0-9]+/g,"-")}))} export default async function Category({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const ps=getCategoryProducts(slug); if(!ps.length)notFound(); return <main className="container py-14"><p className="accent">CATEGORY</p><h1 className="text-4xl font-bold mt-2">{ps[0].category}</h1><p className="muted mt-3">Structured reviews and research for software in this category.</p><div className="grid md:grid-cols-3 gap-5 mt-8">{ps.map(p=><ProductCard key={p.slug} product={p}/>)}</div></main>}