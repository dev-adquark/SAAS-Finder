import type {Metadata} from "next";import {notFound} from "next/navigation";import {categories,getCategoryProducts} from "@/lib/data";import {ProductCard} from "@/components/product-card";export async function generateStaticParams(){return categories.map(c=>({slug:c.toLowerCase()}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const c=categories.find(x=>x.toLowerCase()===slug);return c?{title:c+" software reviews and comparisons",description:"Compare "+c+" software, features, pricing and alternatives."}:{}}export default async function Category({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const c=categories.find(x=>x.toLowerCase()===slug);if(!c)notFound();return <section className="section"><div className="container"><span className="eyebrow">Category hub</span><h1>{c} software</h1><p className="section-intro">Consistent comparison criteria, product reviews and alternatives for this category.</p><div className="grid">{getCategoryProducts(c).map(p=><ProductCard key={p.slug} product={p}/>)}</div></div></section>}