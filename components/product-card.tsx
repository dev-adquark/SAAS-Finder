import Link from "next/link"; import type {Product} from "@/lib/data"; export default function ProductCard({product}:{product:Product}){return <article className="panel p-5"><div className="text-xs accent">{product.category}</div><h3 className="text-xl font-semibold mt-2">{product.name}</h3><p className="muted mt-2 text-sm">{product.tagline}</p><div className="mt-4 flex justify-between"><span className="text-sm">Editorial rating <strong>{product.rating}/5</strong></span><Link className="accent text-sm" href={"/products/"+product.slug}>Read review →</Link></div></article>}