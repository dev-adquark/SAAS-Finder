"use client";import {ArrowUpRight} from "lucide-react";import {trackEvent} from "@/lib/events";export function VendorCTA({product,compact=false}:{product:{slug:string;name:string;officialUrl:string;affiliateUrl?:string;affiliateAvailable:boolean};compact?:boolean}){const href=product.affiliateAvailable&&product.affiliateUrl?product.affiliateUrl:product.officialUrl;return <a className="btn primary" href={href} target="_blank" rel="nofollow sponsored noopener" onClick={()=>trackEvent("outbound_click",{product:product.slug,placement:compact?"compact":"primary"})}>{compact?"Visit vendor":"Check current plans"} <ArrowUpRight size={16}/></a>}