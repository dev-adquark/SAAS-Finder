"use client";import {useMemo,useState} from "react";import Link from "next/link";import type {Product} from "@/lib/data";export function SearchProducts({products}:{products:Product[]}){const[q,setQ]=useState("");const list=useMemo(()=>products.filter(p=>(p.name+" "+p.category+" "+p.tagline).toLowerCase().includes(q.toLowerCase())),[products,q]);return <><div className="searchbar"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search products, categories or use cases..."/></div><div className="grid">{list.map(p=><Link className="card" key={p.slug} href={"/products/"+p.slug}><span className="tag">{p.category}</span><h3 className="product-title">{p.name}</h3><p className="product-desc">{p.tagline}</p><span className="muted">Editorial score {p.rating.toFixed(1)}/5 →</span></Link>)}</div>{!list.length&&<div className="empty">No matching products.</div>}</>}